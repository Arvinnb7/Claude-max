"""اثر افزوده‌ی اندازه‌گیری‌شده — سیستم از نتیجه‌ی کمپین‌های خودش یاد می‌گیرد.

## مسئله

رتبه‌بندی امروز `ارزش × احتمال خرید` است. اما مشتری‌ای که فردا **به‌هرحال**
می‌خرد، احتمال خرید بالایی دارد و تماس با او صفر ارزش تولید می‌کند. چنین
مشتری‌ای امروز صدر فهرست است، و آن یک خطای ساختاری است نه یک تنظیم.

## آنچه باید جایش بنشیند

```
اثر افزوده = نرخ خرید گروه تماس‌گرفته − نرخ خرید گروه کنترل
```

این عدد از داده‌ی خودِ کمپین‌ها می‌آید (فاز ۳). یعنی سیستم دیگر از حدسِ کسی
تغذیه نمی‌کند؛ از نتیجه‌ی سنجیده‌شده‌ی کار خودش تغذیه می‌کند.

## چرا سلولی، نه به‌ازای هر مشتری

برای «این مشتری خاص» اثر قابل اندازه‌گیری نیست — هر نفر یا خرید کرده یا نکرده،
و از یک مشاهده اثر درنمی‌آید. پس اثر در **گروه‌های همگن** اندازه‌گیری می‌شود
و به اعضایشان تعمیم داده می‌شود. مدلِ فردی (فاز ۵ سند) وقتی معنا پیدا می‌کند که
هزاران مشاهده‌ی آزمایشی جمع شده باشد.
"""

from .empirical import (
    BASIS_CELL,
    BASIS_GLOBAL,
    BASIS_KIND,
    BASIS_NONE,
    MIN_CELL_OBSERVATIONS,
    UpliftCell,
    UpliftTable,
    compute_uplift_table,
)
from .snapshots import build_uplift_table, refresh_uplift, save_snapshot

__all__ = [
    "BASIS_CELL",
    "BASIS_GLOBAL",
    "BASIS_KIND",
    "BASIS_NONE",
    "MIN_CELL_OBSERVATIONS",
    "UpliftCell",
    "UpliftTable",
    "build_uplift_table",
    "compute_uplift_table",
    "refresh_uplift",
    "save_snapshot",
]
