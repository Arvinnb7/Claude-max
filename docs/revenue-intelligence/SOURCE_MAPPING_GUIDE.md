# راهنمای نگاشتِ منبع (Source Mapping Guide)

سند §۳۶. **از کد تولید می‌شود** (`tools/gen_reference_docs.py`): نقش‌ها از
`ingest/schema.py`، کلیدواژه‌های تشخیصِ خودکار از `ingest/mapper.py`، برچسب‌ها از
`locale_fa.py`. `tests/test_docs_drift.py` هر `ColumnRole` و نقش‌های الزامی را اینجا پین می‌کند.

## چرخه‌ی نگاشت

1. فایل (اکسل/CSV) بارگذاری می‌شود؛ `SchemaMapper.auto_detect` هر ستون را با کلیدواژه و
   شکلِ محتوا امتیاز می‌دهد و بالای `AUTO_SELECT_THRESHOLD = 0.65` خودکار انتخاب می‌کند.
2. اپراتور نگاشت را تأیید/اصلاح می‌کند و **واحدِ پولِ فایل** و **واحدِ نمایش** را انتخاب می‌کند
   (سیستم واحد را حدس نمی‌زند؛ واحدِ نامعلوم ثبت در دفتر کل را مسدود می‌کند — C00، §۸.۵).
3. `SchemaMapper.apply` فریمِ استاندارد را می‌سازد (ستون‌های جدولِ زیر + ستونِ فنیِ
   `source_row` = شماره‌ی ردیفِ منبع)؛ نبودِ نقشِ الزامی خطا است.
4. **حافظه‌ی نگاشت**: امضای سرستون (`header_signature` — مجموعه‌ی مرتبِ نام‌های نرمال‌شده،
   مستقل از ترتیب و نگارش) در `mapping_profiles` (legacy، upsert) نگه داشته می‌شود تا فایلِ ماهِ بعد
   با همان ساختار، همان نگاشت و واحدها را پیش‌فرض بگیرد.
5. **نگاشتِ نسخه‌دار (برشِ اول)** — §۸.۲: هر نگاشتِ متفاوتی که روی یک امضا نهایی شد یک ردیفِ
   افزودنی در `mapping_profile_versions` می‌گیرد (`version` از ۱)؛ همان نگاشت با همان واحدها
   نسخه‌ی تازه نمی‌سازد (`mapping_hash`). هر بارگذاری — حتی مسدود — `mapping_signature` و
   `mapping_version`ِ خودش را دارد و `GET /api/v1/source-mappings` تاریخچه را می‌دهد.

## نقش‌ها

الزامی: `DATE`، `REVENUE` — بقیه اختیاری‌اند و نبودشان
«سنجیده نشد» می‌سازد، نه خطا.

| نقش | برچسب | ستونِ استاندارد | الزامی | نوع | کاربرد | کلیدواژه‌های تشخیصِ خودکار (نمونه) |
|---|---|---|---|---|---|---|
| `DATE` | تاریخ | `date` | ✓ | — | پایه‌ی هر تحلیلِ زمانی؛ جلالی/میلادی هر دو خوانده می‌شود، در دفتر کل ISO | تاریخ، date، datetime، زمان، تاریخ فاکتور، تاریخ فروش |
| `REVENUE` | درآمد/مبلغ | `revenue` | ✓ | عددی | درآمدِ خط؛ منفی = برگشت (اگر «نوع سند» نباشد، حدس است) | قابل پرداخت، خالص پرداختی، مبلغ نهایی، مبلغ کل، جمع کل، قیمت کل |
| `QUANTITY` | تعداد | `quantity` | — | عددی | مقدار؛ با قیمتِ واحد، درآمدِ خالی را مشتق می‌کند | تعداد، مقدار، کمیت، qty، quantity |
| `UNIT_PRICE` | قیمت واحد | `unit_price` | — | عددی | قیمتِ واحد | قیمت واحد، فی واحد، بهای واحد، نرخ واحد، unit price، فی |
| `PRODUCT` | محصول | `product` | — | برچسبی | نامِ کالا → کالای پایدار (نرمال‌سازیِ نام، اندازه‌ی بسته، برند) | شرح کالا، نام کالا، نام محصول، عنوان محصول، عنوان کالا، شرح محصول |
| `CATEGORY` | دسته‌بندی | `category` | — | برچسبی | دسته‌ی کالا (شکافِ دسته، پیشنهاد) | دسته‌بندی، دسته بندی، گروه کالا، دسته، گروه، category |
| `CUSTOMER_ID` | مشتری | `customer_id` | — | — | کلیدِ خامِ مشتری → مشتری پایدار (با شماره قوی‌تر می‌شود) | نام مشتری، نام خریدار، کد مشتری، شناسه مشتری، مشتری، خریدار |
| `CHANNEL` | کانال فروش | `channel` | — | برچسبی | کانالِ فروش | کانال فروش، کانال، روش فروش، channel، source |
| `REGION` | منطقه | `region` | — | برچسبی | منطقه | منطقه، استان، شهر، ناحیه، region، city |
| `COST` | هزینه | `cost` | — | عددی | بهای تمام‌شده‌ی خط؛ بدون آن سود «سنجیده نشد» | بهای تمام شده، قیمت خرید، هزینه، cost، cogs |
| `ORDER_ID` | شماره سفارش | `order_id` | — | — | شماره‌ی فاکتور → هویتِ پایدارِ خط و سرِ فاکتورِ دوره‌دار؛ بدون آن هر خط یک خرید است | شماره فاکتور، شماره سفارش، کد سفارش، کد فاکتور، شماره سند، فاکتور |
| `DISCOUNT` | تخفیف | `discount` | — | عددی | تخفیف؛ مبلغی یا نسبتی — تفسیر یک‌بار در پاک‌سازی تعیین می‌شود | مبلغ تخفیف، درصد تخفیف، تخفیف، discount |
| `SALESPERSON` | فروشنده | `salesperson` | — | برچسبی | فروشنده | فروشنده، کارشناس فروش، ویزیتور، بازاریاب، salesperson، sales rep |
| `BRANCH` | شعبه | `branch` | — | برچسبی | شعبه (شعبه‌ی محتملِ مشتری) | شعبه، فروشگاه، نمایندگی، مرکز فروش، branch، store |
| `PHONE` | تلفن/موبایل | `phone` | — | — | موبایل → کلیدِ هویتِ قوی + کانالِ پیامک | شماره موبایل، تلفن همراه، موبایل، شماره تماس، همراه، تلفن |
| `EMAIL` | ایمیل | `email` | — | — | ایمیل → کلیدِ هویتِ ذخیره‌شده (پیوند نمی‌سازد؛ L13) | ایمیل، پست الکترونیک، رایانامه، email، mail |
| `DOC_TYPE` | نوع سند (فروش/برگشت) | `doc_type` | — | — | نوعِ سند: برگشتی‌های **اعلام‌شده** به‌جای حدس از علامت (C04، §۸.۵) | نوع سند، نوع فاکتور، نوع تراکنش، doc type، document type |
| `GROSS_AMOUNT` | مبلغ ناخالص (قبل از تخفیف) | `gross_amount` | — | عددی | مبلغِ ناخالص پیش از تخفیف — فقط برای کنترلِ آشتی | مبلغ ناخالص، جمع ناخالص، مبلغ قبل از تخفیف، قیمت کل، gross amount |

## واحدِ پول

| واحدِ فایل | ریال به‌ازای هر واحد |
|---|---|
| `ریال` | 1 |
| `تومان` | 10 |

واحد **انتخابِ اپراتور** است و روی دسته ثبت می‌شود (`file_currency`, `display_currency`,
`rial_per_file_unit`). تبدیل بعد از اعتبارسنجی انجام می‌شود؛ ردیف‌های کنارگذاشته با مبلغِ
اصلیِ فایل می‌مانند (کنترلِ L12 با واحدِ فایل به ریال می‌برد). تخفیفِ **مبلغی** هم تبدیل می‌شود،
تخفیفِ نسبتی نه.

## آنچه در نگاشت **حدس زده نمی‌شود**

* واحدِ پول (C00 مسدود می‌کند) · قراردادِ علامتِ مبهم (C04 مسدود می‌کند) · تفسیرِ تخفیف زیرِ اطمینان.
* ادغامِ مشتری با نام (فقط شماره و کلیدِ خام؛ ایمیل کلیدِ ذخیره‌شده است) — `IDENTITY` در
  `FINANCIAL_CALCULATION_RULES.md` و کنترلِ L13.

## آنچه §۸.۲ می‌خواهد و هنوز نیست

| خواسته‌ی سند | وضعیت |
|---|---|
| شناسه‌ی `source_system` به‌ازای هر منبع | نیست — با یک منبعِ فروش، کلید فقط امضای سرستون است |
| قواعدِ تبدیلِ نسخه‌دار به‌ازای منبع (فراتر از نقش→ستون) | نیست — تبدیل‌ها در `cleaning.py` سراسری‌اند |
| گردشِ تأییدِ نگاشت (پیشنهاد → بازبینی → تصویب) | نیست — اپراتور در همان صفحه تأیید می‌کند و همان لحظه نسخه ثبت می‌شود |
| بازپخشِ یک بارگذاری با نسخه‌ی مشخصِ نگاشت | نیست — نسخه ثبت می‌شود ولی مسیرِ «دوباره با نسخه‌ی n» وجود ندارد |
