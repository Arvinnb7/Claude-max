# یادداشت‌های انتشار و نقطه‌های ایست

سند §۳۶. هدفش یک چیز است: **هر کسی (یا هر جلسه‌ی کاریِ بعدی) با یک نگاه بداند
کجای کار هستیم، چه چیزی تمام شده، و قدمِ بعدی چیست.**

ترتیب از تازه به قدیم.

---

## نقطه‌ی ایست — ۱۴۰۵/۰۶/۱۲ (۲۰۲۶-۰۹-۰۳) · صحتِ دفتر کل و صداقتِ اندازه‌گیری

### چرا این دور

حسابرسیِ ۲۳ عاملی (کاوشگر + ردکننده به‌ازای هر فاز، سه معمار، داور) نشان داد
اعدادِ جدولِ فازها خوش‌بینانه بود و سه چیز پیش از هر ارزشِ تازه‌ای باید درست
می‌شد: دفتر کل صادراتِ هم‌پوشان را دوبار می‌شمرد، تنها «must»ِ نقض‌شده‌ی سند
(§۸.۵) بیرون از هر دروازه‌ای بود، و رتبه‌بندی و گزارش با شواهدِ ناکافی حرف
می‌زدند. لنزِ «ریسک‌محور» داوری را برد (۸/۱۰).

### چه چیزی بسته شد

| گام | چه چیزی |
|---|---|
| P0 | `GET /api/v1/quarantine` گارد و ماسکِ شماره گرفت (نشتِ PII هم‌ارزِ `/api/outbox`) |
| P1 | گاردِ §۸.۵: واحدِ پولِ نامعلوم، علامتِ وارونه، جمعِ ناممکن ⇒ هیچ خطی به دفتر کل نمی‌رود؛ دسته `BLOCKED` با راهِ رفع |
| P2 | هویتِ خطِ فاکتور = فاکتور + کالا + نوع + ترتیب (مهاجرت ۱۷)؛ صادراتِ هم‌پوشان به‌روز می‌کند نه دوبار؛ سرِ فاکتور از همه‌ی خطوط؛ L08/L09 |
| P3 | جدولِ اثر: تخمینِ نوع/کل هم حداقلِ نمونه می‌خواهد، وگرنه ضریب ۱٫۰؛ برچسب تابعِ علامت؛ آستانه‌ی برنامه‌ریز از تنظیم |
| P4 | حکمِ inconclusive عددِ «افزوده» ندارد؛ `observed_difference` با نامِ خودش |
| P5 | خستگیِ تماس (۱۴ روز) در ساخت/خروجی/ارسالِ کمپین و در موتور، از مهرِ تماسِ کمپین‌ها + outbox |
| P6 | «لغو ۱۱» و لیستِ سیاهِ پنل با شماره: `POST/DELETE /api/v1/contact-suppressions` + `/import` |
| P7 | هشدارِ انحرافِ مدل واقعاً شلیک می‌شود (`alerts` در نتیجه‌ی کار، نشان در سلامتِ عملیات) |
| P8 | «کدام‌ها زود منقضی می‌شوند؟»: `sort=expires_at`, `expires_within_days`, `expiring_soon_count` با مرجعِ `as_of` |
| P9 | چهار تستِ «سبزِ مشروط» قطعی شدند + نگهبانِ `pytest.skip` داده‌محور |
| P10 | جدولِ فازها با اعدادِ راستی‌آزمایی‌شده؛ دفترِ مسدودها، RUNBOOK↔ROLLBACK، تصمیم ۸ و تصمیم‌های فاز ۵/۶ راست شدند |

### تصمیم‌های کاربر که اجرا شد

گاردِ §۸.۵ فقط خطاهای مالیِ خطرناک · هویتِ خط = شناسه‌ی سفارش + محتوا (بدون
حذف) · پنجره‌ی خستگی همان ۱۴ روزِ موتور · والدهای جدولِ اثر با همان آستانه‌ی
سلول.

### دو انحرافِ ثبت‌شده از پلن

* مبلغ در کلیدِ خطِ فاکتور **نیست** (پلن: «مقدار + مبلغ») — وگرنه مبلغِ اصلاح‌شده
  همان خط را به‌روز نمی‌کرد و دوبارشماری می‌ماند. ترتیبِ خط جای آن را می‌گیرد.
* پنجره‌ی خستگی از ساعتِ دیوار سنجیده می‌شود، نه `as_of`: تماس رویدادِ
  زمانِ واقعی است؛ سنجشش با تاریخِ داده تماسِ امروز را نمی‌دید. حفره‌ی
  بازتولیدپذیریِ فاز ۲ صریح باز می‌ماند.

### قدمِ بعدی (پیشنهاد)

پرونده‌ی مادام‌العمرِ مشتری از دفتر کل (قهرمان/مدعی با تستِ طلاییِ «مشتریِ غایب
از فایلِ ماهِ دوم»)، سپس خروجیِ روزانه‌ی مدیریتی §۳۸؛ یا دورِ «موتور فرصت»:
چرخه‌ی خریدِ شخصی §۱۳.۲ با holdout زمانی.

---

## نقطه‌ی ایست — ۱۴۰۵/۰۶/۱۱ (۲۰۲۶-۰۹-۰۲) · نردبانِ تخفیف با کف حاشیه و تأیید دستی

### چه چیزی بسته شد

ده گام، ده کامیت (`0326402` … `9610c42`) و یک دورِ بازبینیِ خصمانه بعد از آن، همه روی همان برنچ:

| گام | چه چیزی |
|---|---|
| S0 | فیکسچرهای §۳۴.۳ (وفادارِ تمام‌قیمت / وابسته به تخفیف، مبلغی و نسبتی) + گاردهای خطِ سرخِ ارسال دیگر `skip` نمی‌شوند |
| S1 | مهاجرت ۱۵: `opportunity_offers` + `full_price_share_bp` + `offer_discount_bp` روی اعضا و ارسال‌ها |
| S2 | سهمِ خریدِ تمام‌قیمت ذخیره و طبقه‌بندی شد؛ برچسبِ «همبستگی، نه علّیت» در API اجباری |
| S3 | `GET/PUT /api/v1/offer-policy` — نردبان، آستانه‌ها، و سنجه‌ی «روی چند فرصت اثر دارد» |
| S4 | `filter_offer_policy`: کوچک‌ترین پله‌ای که حاشیه‌ی **پس از** تخفیف ≥ کف بماند؛ بدون نردبان بیت‌به‌بیت مثل قبل |
| S5 | دفترِ آفر + `POST …/offer/{approve|reject}` با بازمحاسبه‌ی حاشیه در لحظه‌ی تأیید + ممیزی |
| S6 | دروازه‌ی ارسال: `{تخفیف}` فقط از آفرِ مصوب؛ بدون تأیید، عضو ساختاراً ارسال نمی‌شود |
| S7 | UI: نردبان کنارِ کف حاشیه، نشان و تأیید روی کارتِ فرصت، سهمِ تمام‌قیمت در پرونده |
| S8 | `EXPERIMENTATION_GUIDE.md` (۱۰ از ۱۵ سندِ §۳۶)، فرمول در `FINANCIAL_CALCULATION_RULES`، بازنویسیِ `IMPLEMENTATION_STATUS` و README |
| S9 | `GET /api/v1/phase5-readiness` + `PUT /api/v1/data-gates` (§۲۹.۶) |

### تصمیم‌های کاربر که اجرا شد

پله‌ها ۵٪/۱۰٪/۱۵٪ فقط نسبتی و **کوچک‌ترینِ مجاز** · تأیید تک‌فرصتی در صندوق
فرصت‌ها · مالک قالب را با `{تخفیف}` می‌نویسد · بدون UI بها و بدون شش سندِ دیگر.

### قاعده‌هایی که این دور بر آن‌ها بنا شد

1. **سیستم فقط پیشنهاد می‌کند.** هیچ تخفیفی بدون تأییدِ انسان ارسال نمی‌شود —
   و این را خودِ مسیرِ ارسال تضمین می‌کند، نه یک پرچم.
2. **کف روی حاشیه‌ی پس از تخفیف** اعمال می‌شود: (m − d)/(1 − d). روی مرز، فرمولِ
   ساده جوابِ غلط می‌داد.
3. **تأییدِ دیروز مبنای امروز نیست.** تغییرِ کف/بها ⇒ «کهنه» و تأیید دوباره.
4. **نبودِ ستونِ تخفیف ⇒ نمی‌دانیم**، نه «هیچ‌وقت تخفیف نگرفته».
5. **همبستگی را علّی جا نزن** (§۲۰.۳ بند ۵) — متنِ غیرعلّی در API اجباری است.

### سه چیزی که تست‌ها گرفتند

* فرصتِ بی‌کالا (نجات از ریزش) با مبنای فقط-کالا همیشه «حاشیه نامعلوم» می‌شد ⇒
  مبنای حاشیه‌ی **خودِ مشتری** اضافه شد.
* علامتِ «کهنه» هنگام ردِ تأیید با استثنا rollback می‌شد ⇒ اول commit، بعد رد.
* فیکسچرِ دستیِ قاعده‌ی دروازه‌ی تماس با ۴۰۹ افتاد ⇒ خودِ دروازه مشتری را
  انتخاب می‌کند.
* سوئیتِ کامل نشان داد **حدِ اندازه‌ی کمپین پیش از دروازه‌ی تماس** اعمال می‌شد:
  عضوِ کنترلِ کمپینِ دیگری که فرصتِ پرارزش‌تری داشت جای یک مجاز را می‌گرفت و
  بعد حذف می‌شد ⇒ کمپینِ کوچک‌تر از خواسته یا ۴۰۹ بی‌دلیل. حالا حد روی
  مجازها اعمال می‌شود (`test_gated_out_contacts_do_not_consume_campaign_slots`).

### بازبینیِ خصمانه‌ی پس از تحویل (مهاجرت ۱۶)

ده عاملِ مستقل روی کلِ دیفِ این دور (`12cd36d..9610c42`) گشتند و هر یافته با یک
ردکننده‌ی جدا راستی‌آزمایی شد؛ **دوازده** یافته تأیید و پیش از این نقطه‌ی ایست
رفع شدند. آن‌هایی که رفتار را عوض کردند:

| یافته | رفع |
|---|---|
| مبنای حاشیه در لحظه‌ی تأیید می‌توانست با مبنای لحظه‌ی پیشنهاد فرق کند (کالا در برابر مشتری) | `margin_basis` و `margin_key` روی هر آفر ذخیره می‌شوند (مهاجرت ۱۶)؛ تأیید روی **همان** مبنا دوباره حساب می‌کند |
| ردیفِ کهنه‌ی نردبانِ برداشته‌شده هنوز قابلِ تأیید بود | وضعیتِ تازه‌ی `withdrawn`: بی‌پیشنهاد ⇒ برداشته‌شده، نه کهنه؛ تأیید، پله را با نردبانِ **امروز** می‌سنجد |
| ردِ تأیید (۴۰۹) نه رویداد داشت نه ممیزی، و یادداشتِ انسان را بازنویسی می‌کرد | رویدادِ `offer_approval_refused` + ردیفِ ممیزی؛ یادداشتِ ردِ انسان دست نمی‌خورد |
| `offer_discount_bp` روی عضو بدون آنکه `{تخفیف}` واقعاً رندر شده باشد مهر می‌خورد | مهر فقط وقتی جای‌گذار در متنِ ارسال‌شده رندر شده |
| اکسلِ کمپین آفرِ یک فرصتِ دلخواهِ عضو را می‌نوشت | همان قاعده‌ی ارسال: فقط فرصتِ برگزیده (بیشترین ارزش) |
| سهمِ تمام‌قیمت به‌ازای فایل، نه مشتری، حساب می‌شد و مرجوعی‌ها در مخرج بودند | پوششِ **هر مشتری** جدا؛ مرجوعی بیرون؛ `full_price_lines` ذخیره می‌شود و کمینه‌ی خطوط روی همان است |
| سنجه‌ی پوششِ بها همه‌ی اسنپ‌شات‌ها را می‌شمرد | فقط آخرین اسنپ‌شاتِ همان کسب‌وکار |

و چند تستِ بی‌اثر یا غایب (`sendable` بی‌مبنا، `margin_by_customer` بی‌تست)
واقعی شدند. جزئیات در `EXPERIMENTATION_GUIDE.md` و `SECURITY_AND_PRIVACY.md`.

### قدمِ بعدی (پیشنهاد)

گزینه‌ی **پ** از پلن: بقایای فاز ۱ — ادغامِ هویتِ پس‌ازواقع، صفِ بازبینی،
`confidence_bp` واقعی، و قواعدِ مسدودکننده‌ی §۸.۵ (امروز `ambiguous_sign` با
وجودِ `C04=FAIL` وارد دفتر کل می‌شود). یا **ب**: پنج سندِ باقی‌مانده‌ی §۳۶.

---

## نقطه‌ی ایست — ۱۴۰۵/۰۶/۰۶ (۲۰۲۶-۰۸-۲۸) · پایان فاز ۶ و نقص‌ها

### چه چیزی بسته شد

هفت گام، به‌ترتیبِ خطر:

| گام | چه چیزی بسته شد |
|---|---|
| **O1** | منعِ پیش‌فرض روی مسیرهای نوشتنی · ممیزی خروجیِ PII · سیم‌کشی توکن در UI |
| **O2** | اجاره‌ی اجرا: «هر (کسب‌وکار، تاریخ) یک اجرا» (§۲۸) |
| **S1** | شش کارِ زمان‌بندی‌شده‌ی §۲۸ + تلاش دوباره‌ی نمایی + صف مرده |
| **S2** | شناسه‌ی درخواست، لاگ ساختاریافته، `metrics`، و `health` واقعی (§۳۲) |
| **S3** | ظرفیت پیگیری تیم (§۲۵) + شعبه‌ی محتملِ مشتری (§۲۴.۵) |
| **S4** | `import_rows_raw` و `import_quarantine` (§۷.۱) |
| **S5** | نُه بُعدِ کیفیت داده (§۸.۵) |
| **S6** | `OPERATIONS_RUNBOOK` · `SECURITY_AND_PRIVACY` · هفت تنظیمِ مستندنشده |

### سه نقصِ واقعی که رفع شدند

1. **دو مسیرِ پرخرجِ باز.** `POST /api/strategy` و `POST /api/campaign` پولِ
   واقعیِ Anthropic خرج می‌کردند و بی‌گارد بودند — در حالی که `security.py` و
   `.env.example` هر دو ادعا می‌کردند بسته‌اند. علتِ ریشه‌ای «فراموشی» نبود،
   **پیش‌فرض** بود؛ حالا گارد روی کلِ برنامه است. حسابرسی دو نشتِ دیگر هم پیدا
   کرد: `GET /api/export` و `GET /api/outbox`.
2. **دو اجرای هم‌زمانِ ممکن.** `write_lock` فقط درون یک پروسه کار می‌کرد.
3. **داده‌ای که فعالانه از بین می‌رفت.** ردیف‌های ردشده‌ی فایل فقط در
   `exclusions.parquet` بودند و هرسِ ۱۸۰ روزه پاکشان می‌کرد. حالا در دفتر کل
   می‌نشینند، با کدِ دلیل و **راهِ اصلاح**.

### قاعده‌هایی که این دور بر آن‌ها بنا شد

1. **باز بودن باید صریح باشد.** مسیرِ باز در فهرستِ سفید با دلیل ثبت می‌شود،
   وگرنه بسته است.
2. **شکستی که دیده نشود، تکرار می‌شود.** کارِ شکست‌خورده در صف مرده می‌ماند.
3. **«شرطش برقرار نبود» شکست نیست** و تلاشِ دوباره نمی‌گیرد.
4. **بُعدی که مبنایش نیست، صفر گزارش نمی‌شود** — `None` یعنی «سنجیده نشد».
5. **ظرفیت و شعبه حدس زده نمی‌شوند.** تنظیم‌نشده ⇒ «بررسی نشد».

### وضعیت فازها بعد از این دور

| فاز | تکمیل |
|---|---|
| ۰ — حسابرسی و پایه‌ی ایمنی | ✅ ۱۰۰٪ |
| ۱ — داده‌ی canonical و Customer 360 | ✅ ~۸۵٪ (قرنطینه و ابعاد کیفیت اضافه شدند) |
| ۲ — موتور فرصت قطعی | ✅ ~۹۰٪ (ظرفیت اپراتور اضافه شد) |
| ۳ — کمپین حلقه‌بسته | ✅ ~۹۵٪ |
| ۴ — مدل‌های پیش‌بین | ✅ ~۹۰٪ |
| ۵ — بهینه‌سازی علّی آفر و قیمت | ❌ ~۲۰٪ — ⛔ **با کد بسته نمی‌شود** |
| ۶ — بهینه‌سازی عملیاتی | ✅ ~۹۰٪ |

### قدمِ بعدی

**نردبانِ تخفیف (§۲۰.۳)** — تصمیمِ ثبت‌شده: مجاز، ولی فقط با کف حاشیه و تأیید
دستی. تستی که امروز «هیچ تخفیفی پیشنهاد نمی‌شود» را پین کرده باید **باریک**
شود نه حذف: «می‌تواند پیشنهاد کند، ولی بدون تأیید انسان ارسال نمی‌شود.»

فاز ۵ همچنان منتظرِ **داده** است، نه کد: مدلِ uplift به‌ازای مشتری هزاران
نتیجه‌ی کمپینِ واقعی می‌خواهد و کششِ قیمت تنوعِ قیمتِ عامدانه.

---

## نقطه‌ی ایست — ۱۴۰۵/۰۶/۰۶ (۲۰۲۶-۰۸-۲۸) · پایان فاز ۴

### وضعیت مخزن

| قلم | مقدار |
|---|---|
| برنچ | `claude/marketing-analytics-automation-kpxttj` |
| کامیتِ این نقطه | `84b9e71` — «N9: سنجش انحراف، تبِ سلامت مدل، و اسناد فاز ۴» |
| تعداد کامیت‌های ارتقا | ۱۹ کامیت از `56a1199` (شروع فاز ۰) |
| تست | ۷۴۳ تست سبز (۱ skip) · `ruff` · `tsc` · `eslint` · `npm run build` سبز |
| زمان اجرای کل تست | ~۴۵ دقیقه (کندترین‌ها `test_memory` و `test_currency`اند) |

### وضعیت فازهای سند (§۳۵)

| فاز | تکمیل | دروازه‌ی پذیرش |
|---|---|---|
| ۰ — حسابرسی و پایه‌ی ایمنی | ✅ ۱۰۰٪ | ندارد |
| ۱ — داده‌ی canonical و Customer 360 | 🔶 ~۶۰٪ | ✅ پاس |
| ۲ — موتور فرصت قطعی | 🔶 ~۸۰٪ | ✅ پاس |
| ۳ — کمپین حلقه‌بسته | ✅ ~۹۵٪ | ✅ پاس |
| ۴ — مدل‌های پیش‌بین | ✅ ~۹۰٪ | ✅ پاس |
| ۵ — بهینه‌سازی علّی آفر و قیمت | ❌ ~۲۰٪ | ⛔ وابسته به داده‌ی واقعی |
| ۶ — بهینه‌سازی عملیاتی | ❌ ~۲۵٪ | ندارد |

اسناد الزامی §۳۶: **۷ از ۱۵** (با همین سند).

### چه چیزی در فاز ۴ ساخته شد

نُه گام، شش کامیت:

* **ویژگی‌های «نقطه‌ی زمانی»** (`src/mktcore/features/`) — بازسازی «آنچه در
  تاریخ T می‌دانستیم» از دفتر کل، با گاردِ نشت در یک نقطه.
* **رجیستری مدل** (`model_runs` + `/api/v1/models/*`) — مدل به‌صورت **JSON**
  ذخیره می‌شود نه pickle، پس بازگشت به نسخه‌ی قبلی یعنی فعال‌کردنِ ردیفِ قبلی.
* **CLV سودمحور** با افق ۹۰/۱۸۰/۳۶۵ و بازه‌ی عدم‌قطعیت — کنارِ CLV درآمدی، نه
  به‌جایش.
* **مدل نهنگ آینده** (§۱۸) و **مدل ریزش** به‌صورت خطرِ گسسته (§۱۶.۳ و §۱۳.۵).
* **اقدام رابطه‌ای بدون عدد ریالی** (§۱۸.۵ و §۳۸).
* **انحراف** (§۲۹.۷) و تبِ **سلامت مدل** (§۲۷.۷).

### قاعده‌هایی که این فاز روی آن‌ها بنا شد

1. **قهرمان/مدعی.** آنچه امروز کار می‌کند قهرمان است؛ مدل تازه مدعی. مدعی جای
   قهرمان را نمی‌گیرد مگر روی holdout **زمانی** و با سنجه‌ی **اقتصادی** از او
   جلو بزند. مدلِ فعال‌نشده هیچ ستونی نمی‌نویسد و هیچ رتبه‌ای را جابه‌جا نمی‌کند.
2. **«داده کافی نبود» هم یک نتیجه است، نه خطا** — با عدد و جدولِ «لازم در برابر
   موجود» در `model_runs` ثبت می‌شود.
3. **نبودِ داده ⇒ `NULL` + دلیلِ فارسی؛ هرگز صفر، هرگز عبورِ بی‌صدا.**

### قراردادهایی که نباید شکسته شوند

`analysis/actions.py` دیفِ صفر · خروجی `next_purchase` ثابت · `clv_rial` و
`p_alive_bp` دست‌نخورده · ترتیبِ صندوق فرصت‌ها بدون سنجش عوض نمی‌شود ·
`PRAGMA user_version = 2` · جداول legacy دست‌نخورده.

### قدمِ بعدی (دامنه‌ی تأییدشده)

**نقص‌های امنیتی + فاز ۶ + قرنطینه.** به‌ترتیب:

1. بستنِ سه مسیرِ بی‌گارد (`POST /api/strategy`، `POST /api/campaign`، خروجی
   اکسل کمپین) — دوتای اول پولِ واقعی Anthropic خرج می‌کنند و سومی فهرست
   شماره‌ی تماس می‌دهد.
2. قفلِ «هر کسب‌وکار/تاریخ یک اجرا» (§۲۸).
3. زمان‌بندِ کامل + تلاشِ دوباره + صف مرده.
4. مانیتورینگ: شناسه‌ی درخواست، لاگ ساختاریافته، `metrics`، و `health` واقعی.
5. ظرفیت اپراتور.
6. `import_rows_raw` و `import_quarantine` — **تنها جایی که امروز داده فعالانه
   از بین می‌رود**: ردیف‌های حذف‌شده‌ی فایل فقط در یک فایل موقتِ نشست می‌مانند و
   سیاست نگه‌داری بعد از ۱۸۰ روز پاکشان می‌کند.
7. پنج بُعدِ کیفیت داده که فقط سیم‌کشی می‌خواهند.
8. `OPERATIONS_RUNBOOK` و `SECURITY_AND_PRIVACY` (محل اجرا: سرور شخصی/VPS).

### تصمیم‌های ثبت‌شده برای آینده

* **تخفیف:** سیستم امروز هرگز تخفیف پیشنهاد نمی‌کند و یک تست همین را پین کرده.
  تصمیم گرفته شد که در گامِ §۲۰.۳ تخفیف مجاز شود **مشروط به کف حاشیه و تأیید
  دستی**. آن تست باید **باریک** شود نه حذف: «می‌تواند پیشنهاد کند، ولی بدون
  تأیید انسان ارسال نمی‌شود.»
* **فاز ۵ با کدنویسی بسته نمی‌شود.** خودِ سند بالای آن نوشته «Deliver only when
  data gates pass». مدلِ uplift به‌ازای مشتری هزاران نتیجه‌ی کمپینِ واقعی
  می‌خواهد و کششِ قیمت تنوعِ قیمتِ عامدانه. تا آن روز، آنچه ساخته می‌شود
  «سنجه‌ی آمادگی» است، نه مدلِ بی‌پشتوانه.

---

## نقطه‌ی ایست — پایان سود ناخالص (`e558c0c`)

بها در فایل فروش نبود و در سیستم دیگری نگه‌داری می‌شد، پس مسیر ورودِ جدا ساخته
شد (`POST /api/v1/costs`). نتیجه: سودِ خط در دفتر کل، سود افزوده‌ی کمپین
(دروازه‌ی پذیرش فاز ۳)، و کفِ حاشیه‌ی قابل‌تنظیم.

قاعده‌ای که از این فاز ماند: **پوششِ ناقصِ بها ⇒ عدد گزارش نمی‌شود.** «عددِ
ناقص بدتر از نبودِ عدد است.»

---

## نقطه‌ی ایست — پایان فازهای ۰ تا ۳ (`df27c8e`)

دفتر کل canonical با هویت پایدار و آشتیِ خودحسابرس، صندوق فرصت‌ها با شواهد
کامل، حلقه‌ی بسته‌ی کمپین با گروه کنترل واقعی، دروازه‌ی مجوز تماس، ارسال مستقیم
پیامک با اقتصاد تماس، و ایمن‌سازی پیش از اولین ارسال واقعی.

باگ‌هایی که در همان ایمن‌سازی پیدا و رفع شدند و ارزشِ ثبت دارند: پیامِ داخلیِ
تیم فروش داشت برای مشتری فرستاده می‌شد · خطای پنل پیامکی با HTTP 200 می‌آمد و
«موفق» ثبت می‌شد · ردیفِ ارسالِ ناموفق هزینه ثبت می‌کرد.
