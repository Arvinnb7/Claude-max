# قرارداد صفر-رگرسیون

> فهرست چیزهایی که این ارتقا **حق نداشت** تغییرشان دهد، و جای اثباتشان.
> هر مورد از تست‌های موجود استخراج شده است؛ اگر یکی بشکند، ارتقا چیزی را
> خراب کرده که کار می‌کرد.

## اصل

هر تغییر باید **افزودنی** باشد: کلید تازه، تب تازه، endpoint تازه، جدول تازه.
هیچ کلید، ستون، برچسب یا کد وضعیتِ موجودی حذف یا تغییر نام نمی‌دهد.

## ۱. APIهای موجود

۱۹ endpoint فعلی با همان مسیر، همان کد وضعیت و همان پیام فارسی می‌مانند:
- ۴۱۳ «سقف مجاز» · ۴۱۰ «بایگانی» · ۴۰۹ گیت FAIL · ۵۰۳ کلید AI ·
  ۴۰۰ «واحد پول»/«اجباری»
- مسیرهای تازه در فضای نام مستقل `/api/v1` نشسته‌اند و هیچ مسیر قدیمی را
  جابه‌جا نکرده‌اند.

**اثبات:** `tests/test_api.py`، `tests/test_api_v1.py::test_existing_endpoints_are_untouched`

## ۲. قرارداد `bundle_to_dict`

- ۷ کلید همیشگی + ۱۸ کلید شرطی.
- کلیدهای تازه (`canonical`) فقط **شرطی و افزودنی**.
- `validation.checks[].id` (نه `check_id`) و کلیدهای فارسی `actions.summary`
  دست‌نخورده.

**اثبات:** `tests/test_pipeline.py`، `tests/test_api.py`،
`tests/test_canonical_hook.py::test_analyze_endpoint_still_returns_the_same_contract`

## ۳. `MetricsBundle` روی دیسک

باندل با pickle ذخیره می‌شود، پس تغییر نام فیلد، نشست‌های قدیمی کاربر را
غیرقابل‌خواندن می‌کند. فقط فیلد تازه با مقدار پیش‌فرض مجاز است (همان الگوی
`getattr` که در `serialize.py` هست).

## ۴. خروجی اکسل و گزارش

- ۶ بخش با نام شیت‌ها و سرستون‌های فارسی دقیق، از جمله ۸ نام سگمنت RFM،
  ردیف «جمع کل» و ترتیب نزولی «ارزش (تومان)».
- گزارش MD با ۱۶ سرفصل و رشته‌های دقیق.

**اثبات:** `tests/test_export.py`، `tests/test_reporting.py`

## ۵. رابط کاربری

- ۱۰ تب موجود با شناسه‌های پایدار (`mkt.tab` در `sessionStorage`).
- تب‌های تازه به **انتها** افزوده شده‌اند، پس شناسه‌ی ذخیره‌شده‌ی کاربر
  همچنان معتبر است.

## ۶. مجموعه‌های ثابت فارسی

۲۰ مجموعه‌ی enum بدون تغییر: وضعیت‌های چرخه و خرید بعدی، ۸ سگمنت،
۴ برچسب تأمین، ۵ نوع اقدام، ۶ نوع مخاطب، ۴ دلیل پیشنهاد،
شناسه‌های کنترل C04–C12/W03/W06/W08، سطل «بدون شعبه»، دلایل حذف ردیف،
کلیدهای payload هوش مصنوعی.

## ۷. اعداد پین‌شده

`total_revenue == 1090` · `n_orders == 199` · `mom_growth == 31/29−1` ·
`nowcast ≈ 3000±150` · `PRAGMA user_version == 2` ·
`len(forecast.yhat) == horizon` · `len(scenarios) == 3` · `sum(share) == 1.0`

## ۸. ترتیب مراحل `run_analysis`

ترتیب ۲۲ مرحله بار معنایی دارد (مثلاً `build_action_list` باید بعد از
`analyze_performance` بیاید تا اقدام‌ها owner داشته باشند). این ارتقا **هیچ
مرحله‌ای به `run_analysis` اضافه نکرد**؛ کار تازه بعد از پایان تحلیل و بیرون
از آن انجام می‌شود.

## ۹. `analysis/actions.py`

این فایل **اصلاً تغییر نکرد**. موتور فرصت‌ها همان تابع را بار دوم با
`per_customer_cap=3, limit=5000` صدا می‌زند؛ `bundle.actions` (سقف ۱) برای
کارت داشبورد و شیت اکسل دست‌نخورده می‌ماند.

**اثبات:** `tests/test_opportunities.py::test_bundle_actions_are_not_mutated_by_the_engine`،
`tests/test_baseline_imports.py::test_baseline_wider_action_list_is_superset`

## ۱۰. لایه‌ی ماندگاری قدیمی

- چهار جدول `sessions`, `jobs`, `outbox`, `mapping_profiles` دست‌نخورده‌اند.
- `PRAGMA user_version` در **۲** ماند.
- `Base.metadata` لایه‌ی جدید اصلاً این جدول‌ها را نمی‌شناسد، پس هیچ عملیات
  metadata نمی‌تواند به آن‌ها دست بزند.

**اثبات:** `tests/test_canonical_schema.py::test_metadata_never_knows_legacy_tables`،
`::test_canonical_migration_does_not_bump_user_version`،
`::test_legacy_store_and_canonical_coexist`

## تغییرات عمدی در رفتار موجود (و دلیلشان)

فقط دو مورد، هر دو **رفع باگ**:

| تغییر | چرا لازم بود |
|---|---|
| `convert_monetary_columns` حالا ستون تخفیفِ **مبلغی** را تبدیل می‌کند | پیش‌تر `discount_total` وقتی واحد فایل ≠ واحد نمایش، ده برابر غلط بود و با درآمد جمع‌پذیر نبود |
| انتخاب parser از امضای بایت به‌جای پسوند | فایل با پسوند غلط یا بدون پسوند اصلاً خوانده نمی‌شد |

هر دو تست اختصاصی دارند و تست‌های موجود همچنان سبزند.

## افزوده‌های فاز ۲ و ۳ — همه افزودنی

| افزوده | اثر روی قرارداد موجود |
|---|---|
| `lifecycle_state` در `customer_features` | ستونی که از قبل وجود داشت و `NULL` بود، حالا پر می‌شود؛ هیچ کلید API حذف یا تغییر نام نداد |
| جدول `customer_lifecycle_events` | جدول تازه؛ هیچ جدول موجودی تغییر نکرد |
| مولد `expansion_gap` | به تاپل `GENERATORS` **اضافه** شد؛ مولد قبلی دست‌نخورده است |
| چهار جدول کمپین | جداول تازه؛ مهاجرت ۴ فقط `create_all` با `checkfirst` است |
| `/api/v1/campaigns*` و `/api/v1/dismiss-reasons` | فضای نام تازه، بدون تداخل با `/api/campaign` موجود (که تولید متن AI است) |
| تب «اثر کمپین‌ها» | به **انتهای** فهرست تب‌ها افزوده شد؛ شناسه‌ی تب ذخیره‌شده‌ی کاربر معتبر می‌ماند |
| `payload["campaign_outcomes"]` در پاسخ تحلیل | کلید شرطی و افزودنی، مثل `canonical` |

`analysis/actions.py` همچنان **صفر تغییر** دارد.

## افزوده‌های فاز ۴ — یادگیری بدون رگرسیون

| افزوده | اثر روی رفتار موجود |
|---|---|
| ضریب اثر روی `score_rial` | بدون داده‌ی آزمایشی ضریب **۱٫۰** است → ترتیب صندوق بیت‌به‌بیت مثل قبل (`test_no_uplift_data_means_multiplier_one`) |
| `filter_uplift` | بدون داده `filter_skip` می‌دهد، نه بلوکه؛ هیچ فرصتی از دست نمی‌رود |
| جدول `uplift_snapshots` | جدول تازه؛ مهاجرت ۵ فقط `create_all` با `checkfirst` |
| `detectable_effect` و `power_note_fa` در گزارش | کلید افزودنی در پاسخ؛ کلید موجودی حذف یا تغییر نکرد |
| `GET /api/v1/uplift` | مسیر تازه |
| `payload["uplift"]` در پاسخ تحلیل | کلید شرطی و افزودنی، مثل `canonical` |
| `MKT_UPLIFT_RANKING=0` | کل یادگیری را خاموش می‌کند و رتبه‌بندی به حالت قبل برمی‌گردد |

## افزوده‌های فاز ۵ (گام یکم) — دروازه‌ی مجوز تماس

| افزوده | اثر روی رفتار موجود |
|---|---|
| `contact/permission.py` (تابع خالص) | ماژول تازه؛ دروازه‌ی خالی هیچ‌کس را مسدود نمی‌کند (`test_empty_gate_allows_everyone_but_admits_it_checked_nothing`) |
| جدول `contact_suppressions` | جدول تازه؛ مهاجرت ۶ فقط `create_all` با `checkfirst`، بدون `ALTER TABLE` |
| `consent_denied` در `build_context` | `has_consent_data` عمداً `False` ماند، پس `filter_consent` برای مشتری عادی همان `filter_skip` را می‌دهد (`test_missing_data_filters_are_skipped_not_passed` دست‌نخورده) |
| گاردِ `/api/sms/send` | کلیدهای موجود (`audience_size`, `حالت_آزمایشی`, `تعداد_کل`, `ارسال‌شده`, `ناموفق`, `توضیح`) دست‌نخورده؛ `مسدودشده` و `دلایل_مسدودی` افزودنی‌اند |
| گاردِ ساخت کمپین | `contact_gate` کلیدِ افزودنی در پاسخ؛ حذف **پیش از** تخصیص بازو تا نسبت گروه کنترل جابه‌جا نشود |
| هدرِ `X-Contact-Suppressed` در خروجی اکسل | هدرِ تازه؛ `Content-Disposition` و محتوای فایل دست‌نخورده |
| `contact_opt_out` در پرونده‌ی مشتری | کلید افزودنی؛ `null` یعنی انصرافی ثبت نشده |
| سه مسیر `/api/v1/contact-suppressions` و `/customers/{id}/opt-out` | مسیرهای تازه |

### تغییر عمدی در رفتار موجود (فاز ۵)

| تغییر | چرا لازم بود |
|---|---|
| `recent_contact_customer_ids` حالا شرط `dry_run = 0` دارد | پیش‌تر یک **پیش‌نمایشِ آزمایشی** هم ردیف outbox می‌نوشت و مشتری را ۱۴ روز «خسته از تماس» می‌کرد، پس فرصت‌هایش ساخته نمی‌شد — بی‌آنکه پیامی برایش رفته باشد |

`outbox_exists_recent` عمداً **تغییر نکرد**: پرسشش «آیا قبلاً ثبت کرده‌ایم؟» است
(ثبتِ آزمایشی هم ثبت است و باید مانع ثبت دوباره شود)، نه «آیا مشتری چیزی گرفته؟».
پس dedupe هفت‌روزه‌ی `run_cycle_scan` و تستش دست‌نخورده‌اند.

`analysis/actions.py` همچنان **صفر تغییر** دارد.

## افزوده‌های فاز ۵ (گام دوم) — طراح آزمایش

| افزوده | اثر روی رفتار موجود |
|---|---|
| `experiments/design.py` (تابع خالص) | ماژول تازه؛ هیچ مسیر موجودی صدایش نمی‌زند |
| `experiments/plan.py` | فقط **می‌خواند**؛ هیچ جدولی نمی‌نویسد و هیچ مهاجرتی لازم نداشت |
| `achievable_effect` در `campaigns/analysis.py` | تابع تازه‌ی افزودنی؛ `required_control_size` و `minimum_detectable_effect` دست‌نخورده |
| `GET /api/v1/experiment-plan` | مسیر تازه |
| `detectable_effect` و `power_note_fa` در UI | این دو کلید **از قبل** در پاسخ API بودند و فقط نمایش داده نشدند؛ هیچ کلیدی به API اضافه نشد |
| بخش «آزمایش بعدی» در تب اثر کمپین‌ها | افزوده‌ی UI؛ اگر برنامه خالی باشد اصلاً رندر نمی‌شود |

هیچ جدول تازه‌ای ساخته نشد و `CANONICAL_SCHEMA_VERSION` در **۶** ماند.
`analysis/actions.py` همچنان **صفر تغییر** دارد.

## افزوده‌های فاز ۵ (گام سوم) — ارسال مستقیم و اقتصاد تماس

| افزوده | اثر روی رفتار موجود |
|---|---|
| `execution/cost.py` | ماژول تازه‌ی خالص؛ هیچ مسیر موجودی تغییر نکرد |
| جدول `campaign_sends` | جدول تازه؛ مهاجرت ۷ فقط `create_all` با `checkfirst`. `outbox` قدیمی **دست‌نخورده** ماند |
| `POST /api/v1/campaigns/{id}/send` | مسیر تازه؛ `/api/sms/send` قدیمی با همان قرارداد سر جایش است |
| `contact_cost*` و `cost_per_incremental_order*` در گزارش | کلیدهای افزودنی؛ بدون ارسال `null` می‌مانند |
| `analyze_campaign(..., contact_cost_rial=…)` | پارامتر **کلیدواژه‌ای با پیش‌فرض `None`**؛ همه‌ی فراخوانی‌های موجود بدون تغییر کار می‌کنند |
| `MKT_SMS_COST_PER_SEGMENT_RIAL` | تنظیم تازه با پیش‌فرض ۳۰۰۰ |
| پنل «ارسال پیامک» در تب اثر کمپین‌ها | افزوده‌ی UI؛ روی کمپین بسته نمایش داده نمی‌شود |

`EXPOSURE_CHANNEL_EXCEL` و رفتار خروجی اکسل هیچ تغییری نکردند؛ کانال تازه
(`sms`) در کنارش نشست.

`analysis/actions.py` همچنان **صفر تغییر** دارد و `PRAGMA user_version` در ۲.

## افزوده‌های فاز ۵ (گام چهارم) — ایمن‌سازی

| افزوده | اثر روی رفتار موجود |
|---|---|
| `security.py` + `MKT_API_TOKEN` | بدون تنظیمِ توکن **هیچ‌چیز بسته نمی‌شود** (رفتار قبلی)، ولی `/api/health` و لاگ صریح هشدار می‌دهند |
| گاردِ مسیرهای نوشتنی | مسیرهای خواندنی همگی باز ماندند؛ داشبورد بدون توکن کار می‌کند |
| `SMSRequest.limit` سقف ۵۰۰۰ گرفت | همان سقفی که مسیر کمپین از قبل داشت؛ مقادیر معمول (≤۱۰۰) دست‌نخورده |
| `confirm` برای `scheduler/run-now` | فقط وقتی تنظیمات به ارسال واقعی منجر شود؛ در حالت آزمایشی رفتار قبلی |
| `business_slug="sample"` برای داده‌ی نمونه | خواندن از `active_business_id` می‌آید، پس دمو همچنان دیده می‌شود |
| `resolve_business_id` | اسلاگِ صریحِ غیرپیش‌فرض همان‌طور جستجو می‌شود؛ فقط `default` به «فضای کاری جاری» ترجمه می‌شود |
| `نمونه_پیام` و `بدون_متن` در پاسخ ارسال | کلیدهای افزودنی |
| حذف fallback به `action_fa` | **تغییر عمدی رفتار**: فرصتِ بدون متن دیگر ارسال نمی‌شود، `skipped` می‌شود و قابل تلاش دوباره می‌ماند |
| تشخیص شکست کاوه‌نگار از بدنه | **تغییر عمدی رفتار**: ارسالِ ناموفق دیگر «موفق» ثبت نمی‌شود |

`analysis/actions.py` همچنان **صفر تغییر** دارد و `PRAGMA user_version` در ۲.
